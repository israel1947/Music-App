import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'platzi.music.curso.com',
  appName: 'platzi-music',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
